/**
 * Shared AI model constants — single source of truth for default model identifiers.
 * All consumers (routes, config, frontend) should import from here.
 */

export const DEFAULT_OPENAI_MODEL = "gpt-5.4-nano";
export const DEFAULT_GEMINI_MODEL = "gemini-3.0-flash";
