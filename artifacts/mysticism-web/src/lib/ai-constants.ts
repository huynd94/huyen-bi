/**
 * Frontend AI model constants — mirrors the backend source of truth at
 * `artifacts/api-server/src/lib/ai-constants.ts`.
 *
 * If default model identifiers change, update the backend file first and then
 * sync this file to match.
 */

export const DEFAULT_OPENAI_MODEL = "gpt-5.4-nano";
export const DEFAULT_GEMINI_MODEL = "gemini-3.0-flash";
